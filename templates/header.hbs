    <header class="header component">
        <div class="header__logo">
            <img class="header__image" src={{assetSrc robot}} alt="">
            <p>{{item1_header}}</p>
        </div>
        <nav>
            <ul class="header__links">
            {{#each navLinks}}
                {{{module this}}}
            {{/each}}
            {{!-- <li class="header__item"><a class="header__link header__btn" href={{linkDestination}}>{{linkText}}</a></li> --}}
            </ul>
        </nav>
    </header>
